# Pre-rendered artefacts

This directory contains pre-rendered HTML viewers, movies, and figures
embedded by the docs pages. They are generated locally and committed —
they are NOT built during the ReadTheDocs build.

See `scripts/README.md` for the regeneration workflow and the layout
of the subdirectories.
